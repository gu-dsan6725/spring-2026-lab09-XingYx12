 For each traces, we can dvide the opration into different layers. For instamc. In question 'The latest nws on artifical intllgence'.  The first step is that the agnt recived the question and begin to search it from DuckDuckgo. The second stp is that the agent start search. It actually had couple of articles that in json format. Finally, the output was organized and print out to the user. From [!](braintrust-metrics.png) we can see that all the actions from the agent belongs the lower level of it. Also, the user input and search process are in a same event loop. And after, a new vent loop was executed to produce the output. Another interest thing is that although the first step takes more time to run. It still cost less tokens than the final step. 